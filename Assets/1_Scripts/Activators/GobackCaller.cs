//using br.com.bonus630.thefrog.Environment;
using UnityEngine;
namespace br.com.bonus630.thefrog.Activators
{
    public class GobackCaller : MonoBehaviour
    {
        //[SerializeField] GameObject platform;
        //bool active = false;
        //private void OnTriggerEnter2D(Collider2D collision)
        //{
        //    if (collision.CompareTag("Player") && !active)
        //    {
        //        GameObject go = Instantiate(platform, transform.position, platform.transform.rotation);
        //        FlipPlatform flipPlatform;
        //        if (go.TryGetComponent<FlipPlatform>(out flipPlatform))
        //        {
        //            active = true;
        //            flipPlatform.Points = new GameObject[] { gameObject, GameObject.Find("ToDuckPoint") };
        //            flipPlatform.MaxTime = 6;
        //        }
        //    }
        //}

    }
}
